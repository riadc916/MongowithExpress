<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit the Chat</title>
</head>
<body>
    <h3>Edit your chat: <%=chat._id%></h3>
    <p>Edit your chat from <b><%=chat.from%></b> to <b><%=chat.to%></b></p>
    <form method="POST" action="/chats/<%=chat._id%>?_method=PUT"> 
        <!-- for method first download method-override in npm  -->
        <textarea name="message" rows="6" cols="50"><%=chat.message%></textarea>
        <button>Edit</button>
    </form>
</body>
</html>     